[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "npm run dev:vite -- --host 127.0.0.1"
  port = 8888
  targetPort = 5173
  functions = "netlify/functions"
  publish = "public"
